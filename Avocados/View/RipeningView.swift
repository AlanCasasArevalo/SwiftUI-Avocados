import SwiftUI

struct RipeningView: View {
    var body: some View {
        Text("RipeningView")
    }
}

struct RipeningView_Previews: PreviewProvider {
    static var previews: some View {
        RipeningView()
    }
}
